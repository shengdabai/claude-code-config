# 🤖 Claude Code 项目规则 (Project Rules)

> 将此文件复制到你的项目根目录，Claude Code 会自动读取并遵循这些规则

## 📋 核心原则

### 语言设置
- **回复语言**: 中文为主，技术术语附英文
- **代码注释**: 中文注释 + 关键逻辑英文注释
- **变量命名**: 使用有意义的英文单词，采用 camelCase 或 snake_case

### 交互方式
- 先给思路/方案 → 再给代码 → 最后说明如何测试
- 代码超过 50 行时分块输出
- 不假设用户知道复杂概念，必要时解释

## 🎓 新手友好模式

### 代码规范
```
1. 每个函数必须有中文 docstring
2. 复杂逻辑添加行内注释
3. 使用 try-catch 处理可能的错误
4. 避免过度工程化，保持简洁
```

### 回复格式
```
📌 思路：[简要说明解决思路]
📝 代码：[实现代码]
🧪 测试：[如何验证代码正确]
⚠️ 注意：[可能的坑点或注意事项]
```

## 🔧 技术偏好

### 前端
- React: 优先使用函数组件 + Hooks
- 样式: 优先 Tailwind CSS，其次 CSS Modules
- 状态管理: Zustand > Redux > Context
- TypeScript: 强烈推荐使用

### 后端
- Node.js: Express 或 Fastify
- Python: FastAPI 或 Flask
- 数据库: 优先 PostgreSQL，SQLite 用于开发

### 移动端
- React Native: Expo 优先
- 原生开发: Swift (iOS) / Kotlin (Android)

## 📁 项目结构建议

```
项目根目录/
├── src/
│   ├── components/    # 可复用组件
│   ├── screens/       # 页面/视图
│   ├── services/      # API 调用
│   ├── utils/         # 工具函数
│   ├── hooks/         # 自定义 Hooks
│   └── types/         # TypeScript 类型
├── tests/             # 测试文件
├── docs/              # 文档
└── CLAUDE.md          # 本文件
```

## 🚫 禁止事项

1. ❌ 不要生成超长的 hash 或二进制内容
2. ❌ 不要一次性输出超过 200 行代码
3. ❌ 不要假设文件存在，先确认再操作
4. ❌ 不要跳过错误处理
5. ❌ 不要使用已废弃的 API 或库

## ✅ 鼓励行为

1. ✅ 主动询问不清楚的需求
2. ✅ 提供多种解决方案供选择
3. ✅ 完成后主动建议下一步
4. ✅ 遇到错误时解释原因和修复方法
5. ✅ 推荐最佳实践和设计模式

## 🔍 调试辅助

当遇到错误时：
1. 首先解释错误的含义
2. 分析可能的原因（列出 2-3 个）
3. 提供修复代码
4. 说明如何避免类似错误

## 📚 学习模式

当用户说"解释一下"或"学习模式"时：
- 像给初学者讲课一样解释
- 使用比喻和类比
- 从基础概念讲起
- 提供简单的示例代码

## 🎯 快捷指令

| 指令 | 含义 |
|------|------|
| `简单版` | 提供最简洁的实现 |
| `详细版` | 带完整注释的版本 |
| `解释` | 详细解释代码逻辑 |
| `优化` | 优化现有代码 |
| `测试` | 生成测试代码 |
| `重构` | 重构代码结构 |

---

*此文件由 Claude Code 配置工具生成，可根据项目需求自定义修改*

